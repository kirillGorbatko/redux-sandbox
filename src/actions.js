export const inc = () => ({ type: 'INC' });
export const dec = () => ({ type: 'DEC' });

export const rndp = (payload) => ({type: 'RNDP', payload});
export const rndm = (payload) => ({type: 'RNDM', payload});
